
spring:
  datasource:
    url: jdbc:mysql://83.8.220.155/wildcards_schema?useSSL=false&serverTimezone=UTC
    username: bestuser
    password: bestuser

  thymeleaf:
    prefix: classpath:/templates/
    check-template-location: true
    cache: false

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

server:
  port: 8080