
spring:
  datasource:
    url: jdbc:mysql://83.8.71.49/wildcards_schema?useSSL=false&serverTimezone=UTC
    username: bestuser
    password: bestuser

  thymeleaf:
    prefix: classpath:/templates/
    check-template-location: true
    cache: false

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080